/api/events/host:
  get:
    summary: Get events hosted by current user
    tags: [Events]
    responses:
      200:
        description: List of hosted events
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: '../components/schemas/Event.yaml'
      401:
        description: Unauthorized
